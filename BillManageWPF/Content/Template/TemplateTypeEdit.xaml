<Window x:Class="BillManageWPF.Content.Template.TemplateTypeEdit"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="票夹编辑页面" Height="110" Width="300" WindowStartupLocation="CenterScreen" 
        ResizeMode="CanResizeWithGrip" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5"/>
            <RowDefinition Height="55*"/>
            <RowDefinition Height="34*"/>
            <RowDefinition Height="5"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Label Content="请输入名称:" VerticalContentAlignment="Center" Margin="3" />
            <TextBox Name="txtTypeName" Text="" VerticalContentAlignment="Center" Margin="3,3,3,3" MinWidth="200"/>
        </StackPanel>

        <StackPanel Margin="0,1,20,0" Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right">
            <Button Name="btnQueDing" Content="确定" Click="btnQueDing_Click" />
            <Button Name="btnQuXiao" Content="取消" Click="btnQuXiao_Click" />
        </StackPanel>
    </Grid>
</Window>
